# OpenAI or an OpenAI-compatible endpoint.
OPENAI_API_KEY=
OPENAI_MODEL=
# OPENAI_BASE_URL=

# Optional GitHub token authorized for Copilot SDK requests.
# Omit this to use the SDK's locally signed-in GitHub user.
# COPILOT_GITHUB_TOKEN=
COPILOT_MODEL=gpt-5
